{
  "name": "sudhanshutheone.com",
  "private": true,
  "type": "module",
  "scripts": {
    "docs:dev": "node tools/generate-section-indexes.mjs && vitepress dev docs",
    "docs:build": "node tools/generate-section-indexes.mjs && vitepress build docs",
    "docs:preview": "vitepress preview docs"
  },
  "devDependencies": {
    "vitepress": "^1.6.0"
  }
}
